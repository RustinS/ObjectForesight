# Core
torch
numpy
einops
hydra-core==1.3.2
omegaconf
opencv-python
matplotlib
tqdm
rich>=13.7

open3d
fast_pytorch_kmeans
psutil
huggingface_hub
timm
# numpy 2.x may be incompatible with open3d; pin if needed:
# numpy==1.26.4
# flash-attention (optional):
# pip install git+https://github.com/Dao-AILab/flash-attention.git
# pytorch3d (install via conda): conda install -c pytorch3d pytorch3d

# FLASH_ATTN_CUDA_ARCHS=90 FLASH_ATTENTION_FORCE_BUILD=TRUE MAX_JOBS=8 pip install \
#     --no-deps \
#     --force-reinstall \
#     --no-cache-dir \
#     --no-binary flash-attn \
#     --no-build-isolation \
#     -v \
#     flash-attn

# pip install "git+https://github.com/facebookresearch/pytorch3d.git@stable"